/**
 * Assignment operations
 */

// Store a value in a variable with the assignment operator, =

// myVariable = 5 assigns the Number value 5 to myVariable.

var myVar;
myVar = 7;

// You can assign the value of this variable to another variable now.

var myNum;
myNum = myVar; // myNum now contains the value 7.

// Assigns the contents of a to variable b.

// Commonly, we initialize variables.

var myVar = 0; // Initialized to 0.

/**
 * Understanding uninitialized variables.
 */

 // Initially declared variables are undefined.

var a = 5;
var b = 10;
var c = "I am a"

a = a + 1;
b = b + 5;
c = c + " String!";